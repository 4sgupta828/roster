# Roster deployment config (single vertical per deployment)
ROSTER_ACTIVE_VERTICAL=roster
ROSTER_PROVIDER_MODE=replay          # replay (free) | record | live
ROSTER_LLM_MODEL=claude-sonnet-5
ROSTER_CASSETTE_ROOT=evals/cassettes
ROSTER_CORPUS_DSN=postgresql://user:pass@localhost:5432/roster
# Secrets (record/live only — never commit real values):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
TAVILY_API_KEY=
